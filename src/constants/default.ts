import { FILE_DOMAIN } from "./FileDomain";

// PostType별 색상 - 바다 컨셉
export const DEFAULT_THUMBNAIL = FILE_DOMAIN + "public/system/default-thumbnail.png";

// 프로필 기본 이미지 - 바다 컨셉
export const DEFAULT_PROFILE_IMAGE = FILE_DOMAIN + "public/system/default-profile.png";

// 사용 예시
// <img src={DEFAULT_PROFILE_IMAGE} alt="프로필" />
// style={{ backgroundImage: `url(${DEFAULT_PROFILE_IMAGE})` }}